% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pk_helper.R, R/pk_param.R
\name{pk_helpers}
\alias{pk_helpers}
\alias{cv}
\alias{geom_cv}
\alias{geom_mean}
\alias{fmt_3sig}
\alias{fmt_pct}
\alias{pk_imputation_rules}
\alias{pk_param_metadata}
\alias{pk_param_sort}
\title{PK Summary Statistic Helpers}
\usage{
cv(x)

geom_cv(x)

geom_mean(x, na.rm = TRUE)

fmt_3sig(x)

fmt_pct(x)

pk_imputation_rules(stat_val, label, blq_ratio, postdose, rule = "1/3")

pk_param_metadata()

pk_param_sort(pk_data, key_var = "PARAMCD")
}
\arguments{
\item{x}{(\code{numeric})\cr numeric vector.}

\item{na.rm}{(\code{logical(1)})\cr whether to remove \code{NA} values. Default is \code{TRUE}.}

\item{stat_val}{(\code{character(1)})\cr formatted statistic value.}

\item{label}{(\code{character(1)})\cr label identifying the statistic type (e.g. \code{"Median"}, \code{"Max"}).}

\item{blq_ratio}{(\code{numeric(1)})\cr proportion of BLQ observations (between 0 and 1).}

\item{postdose}{(\code{logical(1)})\cr whether the timepoint is post-dose. The 1/3
rule keeps a separate set of reportable statistics for pre-dose and
post-dose timepoints.}

\item{rule}{(\code{character(1)})\cr imputation rule to apply. One of \code{"1/3"} (default) or \code{"1/2"}.}

\item{pk_data}{(\code{data.frame})\cr pharmacokinetic data frame.}

\item{key_var}{(\code{string})\cr column in \code{pk_data} holding the PK parameter code,
merged against the reference from \code{\link[=pk_param_metadata]{pk_param_metadata()}}. Default is \code{"PARAMCD"}.}
}
\value{
\code{cv()}, \code{geom_cv()}, and \code{geom_mean()} return a single numeric value.
\code{geom_mean()} returns \code{NA_real_} if there are no usable values or if any
non-\code{NA} value is <= 0.

\code{fmt_3sig()} and \code{fmt_pct()} return a \code{character} vector the same length as
\code{x}, with \code{NA_character_} where \code{x} is \code{NA} or non-finite.

\code{pk_imputation_rules()} returns a \code{character} string: the original
\code{stat_val}, \code{"ND"} (not determined), or \code{"NE"} (not estimable).

\code{pk_param_metadata()} returns a \code{data.frame} of PK parameters. \code{pk_param_sort()}
returns \code{pk_data} with ordered \code{PARAM} (and \code{TLG_DISPLAY}) factors.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

\strong{These helpers are intended primarily for pharmacokinetic (PK) concentration
summaries.} Together they cover the pieces needed to build PK concentration
tables:
\itemize{
\item \code{cv()} -- coefficient of variation (\%).
\item \code{geom_cv()} -- geometric coefficient of variation (\%).
\item \code{geom_mean()} -- geometric mean.
\item \code{fmt_3sig()} -- format numbers to 3 significant figures.
\item \code{fmt_pct()} -- format numbers to a single decimal place.
\item \code{pk_imputation_rules()} -- apply BLQ display rules to a formatted statistic.
\item \code{pk_param_metadata()} -- reference table of PK parameter codes and labels.
\item \code{pk_param_sort()} -- order a PK data frame by the conventional parameter order.
}
}
\details{
\code{pk_imputation_rules()} applies BLQ (Below Limit of Quantification) imputation
rules to a formatted PK summary statistic based on the proportion of BLQ
observations and dosing timing. When too many observations are BLQ, summary
statistics that cannot be meaningfully reported are replaced by \code{"ND"} (not
determined), and a geometric mean that could not be computed is replaced by
\code{"NE"} (not estimable).

\code{pk_param_metadata()} returns the reference table mapping PK parameter codes
(\code{PARAMCD}) to their label (\code{PARAM}), display label (\code{TLG_DISPLAY}), matrix,
and display order (\code{TLG_ORDER}). \code{pk_param_sort()} uses this reference to set
the \code{PARAM} (and \code{TLG_DISPLAY}) factor levels of a PK data frame so tables and
listings follow the conventional PK parameter order.
}
\examples{
cv(c(1, 2, 3, 4, 5))

geom_cv(c(1, 2, 3, 4, 5))

geom_mean(c(1, 2, 3, 4, 5))

fmt_3sig(0.001234)
fmt_3sig(123456)
fmt_3sig(c(0.001234, NA, 123456))

fmt_pct(45.678)
fmt_pct(c(45.678, NA))

pk_imputation_rules("1.23", "Mean", blq_ratio = 0.5, postdose = TRUE, rule = "1/3")

pk_reference <- pk_param_metadata()
head(pk_reference)

\dontrun{
adam <- syntheticadam::get_data()
pk_ordered_data <- pk_param_sort(adam$adpp)
}

}
