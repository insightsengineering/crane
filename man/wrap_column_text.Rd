% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrap_column_text.R
\name{wrap_column_text}
\alias{wrap_column_text}
\title{Wrap Text in a gtsummary Table Column}
\usage{
wrap_column_text(x, width = 50, column = "label")
}
\arguments{
\item{x}{(\code{gtsummary})\cr
A \code{gtsummary} object.}

\item{width}{(\code{integer})\cr
The maximum number of characters before a line break is inserted. Defaults
to 50.}

\item{column}{(\code{character})\cr
The name of the column in \code{x$table_body} to apply the text wrapping.
Defaults to \code{"label"}, which is typically the first column.}
}
\value{
A modified \code{gtsummary} object with text wrapping applied.
}
\description{
Wraps text within a specific column of a \code{gtsummary} table to prevent layout
engine issues during document generation (e.g., squished final columns).
}
\examples{
library(gtsummary)

# Create a table with intentionally long labels
tbl <- trial |>
  dplyr::mutate(
    trt = factor(
      trt,
      labels = c("Drug A: A very long string that will wrap",
                 "Drug B: Another extremely long string to test")
    )
  ) |>
  tbl_summary(by = trt, include = age)

# Wrap the label column at 20 characters
tbl_wrapped <- wrap_column_text(tbl, width = 20)

}
