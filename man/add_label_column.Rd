% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_label_column.R
\name{add_label_column}
\alias{add_label_column}
\title{Add a Label Column to a Table}
\usage{
add_label_column(
  x,
  value,
  header = "Value",
  label_header = NULL,
  .after = NULL,
  indent = 0L
)
}
\arguments{
\item{x}{(\code{gtsummary})\cr
a gtsummary object.}

\item{value}{(\code{expression})\cr
expression evaluated with \code{\link[dplyr:mutate]{dplyr::mutate()}} against \code{x$table_body} to fill
the new \code{label0} column. May reference any column in \code{x$table_body} (e.g.
\code{label}, \code{row_type}) and \code{\link[dplyr:row_number]{dplyr::row_number()}}. \code{NA} rows render blank.}

\item{header}{(\code{string})\cr
header for the new \code{label0} column. Default is \code{"Value"}.}

\item{label_header}{(\code{string})\cr
header for the existing \code{label} column. Default is \code{NULL}, which leaves the
current \code{label} header unchanged.}

\item{.after}{(\code{string})\cr
name of the column after which \code{label0} is placed. Default is \code{NULL}, which
places \code{label0} directly before \code{label} (the \code{\link[=tbl_shift]{tbl_shift()}} layout, value
column on the left). Use \code{.after = "label"} to place it on the right.}

\item{indent}{(\code{integer})\cr
indentation applied to the \code{label} column via \code{\link[gtsummary:modify_indent]{gtsummary::modify_indent()}}.
Default is \code{0L}, matching \code{\link[=tbl_shift]{tbl_shift()}}, so value rows sit flush left once
the stratum label moves to \code{label0}. Use \code{NULL} to leave the current
\code{label} indentation unchanged (e.g. to keep a nested table's indentation).}
}
\value{
a gtsummary object with a \code{label0} column added next to \code{label}.
}
\description{
Add a new column, \code{label0}, to the left of a \code{{gtsummary}} table's \code{label}
column, and move part of the row content into it. This splits a single label
column into two: \code{label0} (the new label column) and \code{label}.

The contents of \code{label0} are set by the \code{value} expression, which is
evaluated against the table body. Any per-row content can be placed there,
for example a stratum label on the first row only, a folded category, or a
constant such as \code{"n"}.

This is the layout \code{\link[=tbl_shift]{tbl_shift()}} builds internally with
\code{strata_location = "new_column"}, provided here as a standalone step so other
count and shift tables can reuse it instead of re-writing the
\code{modify_table_body()} / alignment / indentation sequence.
}
\examples{
library(dplyr, warn.conflicts = FALSE)

tbl <- gtsummary::trial |>
  select(trt, grade) |>
  gtsummary::tbl_summary(by = trt, include = grade) |>
  gtsummary::remove_row_type(type = "header")

# place a stratum label on the first row, blank elsewhere
add_label_column(
  tbl,
  value = ifelse(dplyr::row_number() == 1L, "Grade", NA_character_),
  header = "Group"
)

}
\seealso{
\code{\link[=tbl_shift]{tbl_shift()}}
}
