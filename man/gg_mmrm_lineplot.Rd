% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_mmrm_lineplot.R
\name{gg_mmrm_lineplot}
\alias{gg_mmrm_lineplot}
\title{Create MMRM Line Plot}
\usage{
gg_mmrm_lineplot(
  mmrm_df,
  arm,
  visit,
  error_bar = c("ci", "se"),
  dodge_width = 0.15,
  hline = 0,
  legend_pos = c(0.02, 0.02)
)
}
\arguments{
\item{mmrm_df}{(\code{data.frame})\cr
A tidy data frame containing the MMRM results. Usually the output of \code{\link[=get_mmrm_results]{get_mmrm_results()}}.}

\item{arm}{(\code{string})\cr
The column in \code{mmrm_df} that identifies the treatment arms.}

\item{visit}{(\code{string})\cr
The column in \code{mmrm_df} that identifies the visits.}

\item{error_bar}{(\code{string})\cr
Type of error bars to display. Either \code{"ci"} (95\% Confidence Interval) or \code{"se"} (Standard Error).
Default is \code{"ci"}.}

\item{dodge_width}{(\code{numeric})\cr
The amount to jitter the x-axis points to prevent overlapping error bars. Default is \code{0.15}.}

\item{hline}{(\code{numeric} or \code{NULL})\cr
The y-intercept for a horizontal reference line. Set to \code{NULL} to remove. Default is \code{0}.}

\item{legend_pos}{(\code{numeric} vector or \code{string})\cr
The position of the legend. To place it inside the plot on the bottom left, use \code{c(0.02, 0.02)}.
Can also be standard string positions like \code{"bottom"}, \code{"right"}, \code{"none"}.}
}
\value{
A \code{ggplot} object.
}
\description{
Generates a standardized line plot for Mixed-Effect Repeated Measures Model (MMRM)
analysis. It displays adjusted means (change from baseline)
along with either 95\% Confidence Intervals (CI) or Standard Errors (SE) over time.
}
\examples{
\dontshow{if (identical(Sys.getenv("NOT_CRAN"), "true") && requireNamespace("mmrm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
library(mmrm)
fv_dt <- mmrm::fev_data |>
  dplyr::mutate(
    ARMCD = factor(ARMCD)
  )
# Fit an MMRM model using the FEV data
fit_mmrm <- mmrm::mmrm(
  formula = FEV1 ~ RACE + SEX + ARMCD * AVISIT + us(AVISIT | USUBJID),
  data = fv_dt
)
mmrm_results <- get_mmrm_results(fit_mmrm, arm = "ARMCD", visit = "AVISIT", conf_level = 0.95)

# Create a plot with SE bars, jittered by 0.2, legend inside bottom-left
my_plot <- gg_mmrm_lineplot(
  mmrm_df = mmrm_results,
  arm = "ARMCD",
  visit = "AVISIT",
  error_bar = "se", # Switch to "ci" for Confidence Intervals
  dodge_width = 0.2, # Jitters the x-axis to prevent overlap
  legend_pos = c(0.02, 0.02)
)

print(my_plot)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_mmrm_results]{get_mmrm_results()}} to get the MMRM results, and \code{\link[=tbl_mmrm]{tbl_mmrm()}} for a summary table of the MMRM results.
}
