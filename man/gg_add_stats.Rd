% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_utils.R
\name{gg_add_stats}
\alias{gg_add_stats}
\title{Add Error Bars to a ggplot2 Object}
\usage{
gg_add_stats(
  gg_plt,
  stat = c("mean", "median"),
  variability = c("sd", "se", "ci", "iqr", "none"),
  conf_level = 0.95
)
}
\arguments{
\item{gg_plt}{(\code{ggplot})\cr
The base \code{ggplot2} object to modify.}

\item{stat}{(\code{string})\cr
Primary statistic to calculate (\code{"mean"} or \code{"median"}).}

\item{variability}{(\code{string})\cr
Measure of variability (\code{"sd"}, \code{"se"}, \code{"ci"}, \code{"iqr"}, or \code{"none"}).}

\item{conf_level}{(\code{numeric})\cr
Confidence level for \code{"ci"} (default \code{0.95}).}
}
\value{
A modified \code{ggplot2} object with an added error bar layer.
}
\description{
Enhances an existing \code{ggplot2} object by adding a \code{stat_summary} layer
that displays a central tendency (mean or median) alongside a specified
measure of dispersion.
}
\examples{
\dontrun{
# Create a base plot with realistic data
p <- ggplot(mtcars, aes(x = factor(cyl), y = mpg)) +
  geom_point(alpha = 0.5)

# Add mean and 95\% CI error bars
gg_add_stats(
  gg_plt = p,
  stat = "mean",
  variability = "ci"
)
}

}
\keyword{internal}
