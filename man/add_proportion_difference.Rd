% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_proportion_difference.R,
%   R/add_proportion_odds_ratio.R
\name{add_proportion_difference}
\alias{add_proportion_difference}
\alias{add_proportion_odds_ratio}
\title{Proportion Comparisons}
\usage{
add_proportion_difference(
  x,
  reference = NULL,
  test = c("chisq", "fisher", "cmh"),
  strata = NULL,
  conf.level = 0.95,
  correct = TRUE,
  test_correct = FALSE,
  header = NULL,
  diff_label = "Difference in Response rate (\%)",
  pvalue_label = NULL,
  ci_label = NULL,
  estimate_fun = label_roche_number(digits = 1),
  pvalue_fun = label_roche_pvalue()
)

add_proportion_odds_ratio(
  x,
  reference = NULL,
  strata = NULL,
  conf.level = 0.95,
  or_label = NULL,
  estimate_fun = label_roche_ratio()
)
}
\arguments{
\item{x}{(\code{tbl_proportion})\cr
a table created by \code{\link[=tbl_proportion]{tbl_proportion()}} on a binary variable (built with the
\code{value} argument).}

\item{reference}{(\code{string})\cr
value of the \code{by} variable used as reference. Defaults to the first level.}

\item{test}{(\code{string})\cr
test for the p-value: \code{"chisq"} (Chi-squared, default), \code{"fisher"}
(Fisher's exact), or \code{"cmh"} (Cochran-Mantel-Haenszel, requires \code{strata}).}

\item{strata}{(\code{string})\cr
optional stratification variable; when supplied the p-value uses CMH and
the default header becomes \code{"Stratified Analysis"}. Default \code{NULL}.}

\item{conf.level}{(\code{scalar})\cr
confidence level. Default \code{0.95}.}

\item{correct}{(\code{flag})\cr
continuity correction for the difference confidence interval (the
"Wald, with correction" interval from \code{\link[stats:prop.test]{stats::prop.test()}}). Default
\code{TRUE}.}

\item{test_correct}{(\code{flag})\cr
Yates continuity correction for the chi-squared test p-value. Default
\code{FALSE}, matching the NEST 1.0 catalog (RSPT01). Has no effect on the
Fisher or CMH tests.}

\item{header, diff_label, pvalue_label, ci_label}{(\code{string})\cr
row labels. \code{header} defaults to \code{"Unstratified Analysis"} /
\code{"Stratified Analysis"}; \code{pvalue_label} and \code{ci_label} are derived from
\code{test}, \code{conf.level} and \code{correct} when \code{NULL}.}

\item{estimate_fun, pvalue_fun}{(\code{function})\cr
formatters for the difference/CI values and the p-value.}

\item{or_label}{(\code{string})\cr
label for the odds-ratio row. Default \code{"Odds Ratio (95\% CI)"}.}
}
\value{
the input \code{tbl_proportion} with comparison rows appended.
}
\description{
Add between-group comparison rows to a \code{\link[=tbl_proportion]{tbl_proportion()}} table.
\itemize{
\item \code{add_proportion_difference()} adds a block with the difference in response
rates, its confidence interval, and a test p-value.
\item \code{add_proportion_odds_ratio()} adds an odds-ratio row (with confidence
interval).
}

Both verbs compare every non-reference group against \code{reference}. Supplying
\code{strata} switches the p-value (and odds ratio) to their stratified
(Cochran-Mantel-Haenszel) counterparts, so an unstratified and a stratified
block can be stacked by calling the verb twice.

All statistical choices (test, method, confidence level, continuity
correction) live on these functions. Row wording is overridable through the
label arguments.
}
\section{Functions}{
\itemize{
\item \code{add_proportion_odds_ratio()}: Add an odds-ratio row (with confidence interval) comparing each non-reference
group to \code{reference}. Unstratified odds ratios come from logistic regression
(\code{\link[stats:glm]{stats::glm()}}, via \code{\link[cardx:ard_regression_basic]{cardx::ard_regression_basic()}}); when \code{strata} is
supplied the common (CMH) odds ratio from \code{\link[stats:mantelhaen.test]{stats::mantelhaen.test()}} is used.

}}
\examples{
set.seed(1)
df <- data.frame(
  arm = factor(sample(c("A", "B", "C"), 200, TRUE), levels = c("A", "B", "C")),
  rsp = sample(c(TRUE, FALSE), 200, TRUE)
)
tbl_proportion(df, "rsp", "arm", value = TRUE) |>
  add_proportion_difference(reference = "A") |>
  add_proportion_odds_ratio(reference = "A")

}
\seealso{
\code{\link[=tbl_proportion]{tbl_proportion()}}
}
