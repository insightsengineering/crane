% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reverse_difference_ci.R
\name{reverse_rate_difference}
\alias{reverse_rate_difference}
\title{Reverse Rate Difference}
\usage{
reverse_rate_difference(x)
}
\arguments{
\item{x}{(\code{character})\cr
A character vector of rate difference values, possibly with suffixes like "\%".}
}
\value{
A character vector with negated numeric values.
}
\description{
Negates numeric rate difference values while preserving any suffix (e.g., "\%").
This is useful when \code{gtsummary::add_difference_row()} computes \code{reference - arm}
but you need \code{arm - reference}.
}
\examples{
# Basic usage with percentage suffix
reverse_rate_difference(c("5.0\%", "-3.2\%", "0.0\%"))

# Handles NA and empty strings
reverse_rate_difference(c("2.5\%", NA, "", "-1.0\%"))

# Works with values without suffix
reverse_rate_difference(c("10.0", "-5.5"))
}
\seealso{
Usually used together with \code{\link[=reverse_ci]{reverse_ci()}} for reversing confidence intervals; see examples there
for usage with \code{gtsummary::modify_table_body()}.
}
