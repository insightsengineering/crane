% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard_tabulate_abnormal_by_baseline.R
\name{ard_tabulate_abnormal_by_baseline}
\alias{ard_tabulate_abnormal_by_baseline}
\title{ARD Tabulate Abnormality by Baseline Status}
\usage{
ard_tabulate_abnormal_by_baseline(
  data,
  postbaseline,
  baseline,
  abnormal,
  id = "USUBJID",
  by = NULL,
  strata = NULL
)
}
\arguments{
\item{data}{(\code{data.frame})\cr
A data frame containing the clinical results.}

\item{postbaseline}{(\code{\link[dplyr:dplyr_tidy_select]{tidy-select}})\cr
Column name of the post-baseline reference range indicator (e.g., \code{ANRIND}).}

\item{baseline}{(\code{\link[dplyr:dplyr_tidy_select]{tidy-select}})\cr
Column name of the baseline reference range indicator (e.g., \code{BNRIND}).}

\item{abnormal}{(\code{list})\cr
A named list of abnormalities (e.g., \code{list(Low = c("LOW", "LOW LOW"))}).
The name is used as the level label, and the vector contains the values in
\code{postbaseline} and \code{baseline} that define the abnormality.}

\item{id}{(\code{\link[dplyr:dplyr_tidy_select]{tidy-select}})\cr
Column name for the subject identifier. Defaults to \code{"USUBJID"}.}

\item{by}{(\code{\link[dplyr:dplyr_tidy_select]{tidy-select}})\cr
Optional column names for grouping variables (e.g., \code{TRTA}).}

\item{strata}{(\code{\link[dplyr:dplyr_tidy_select]{tidy-select}})\cr
Optional column names for additional stratification.}
}
\value{
An ARD data frame of class 'card'.
}
\description{
This function creates an Analysis Results Data (ARD) object counting participants
with abnormal assessments, stratified by their baseline status. For each abnormality
(e.g., "Low", "High"), it calculates statistics for three tiers:
\enumerate{
\item Patients \strong{Not Abnormal} at baseline.
\item Patients \strong{Abnormal} at baseline.
\item \strong{Total} (all patients with a post-baseline assessment).
}
}
\examples{
# Example usage with ADLB-like data
adlb <- cards::ADLB
ard_tabulate_abnormal_by_baseline(
  data = adlb,
  postbaseline = LBNRIND,
  baseline = BNRIND,
  abnormal = list(Low = "LOW", High = "HIGH"),
  by = TRTA
)
}
