% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_utils.R
\name{gg_get_summary_stats}
\alias{gg_get_summary_stats}
\title{Generate Summary Statistic Function for ggplot2}
\usage{
gg_get_summary_stats(
  stat = c("mean", "median"),
  variability = c("sd", "se", "ci", "iqr", "none"),
  conf_level = 0.95
)
}
\arguments{
\item{stat}{(\code{string})\cr
Primary statistic to calculate (\code{"mean"} or \code{"median"}).}

\item{variability}{(\code{string})\cr
Measure of variability (\code{"sd"}, \code{"se"}, \code{"ci"}, \code{"iqr"}, or \code{"none"}).}

\item{conf_level}{(\code{numeric})\cr
Confidence level for \code{"ci"} (default \code{0.95}).}
}
\value{
A \code{function} that takes a numeric vector and returns a \code{data.frame}
with \code{y}, \code{ymin}, and \code{ymax}.
}
\description{
A function factory that produces a summary closure compatible with
\code{ggplot2::stat_summary(fun.data = ...)}. It computes a central tendency
(mean or median) alongside a specified measure of dispersion.
}
\examples{
\dontrun{
# Generate a closure for Mean and 90\% CI
my_summary_fun <- gg_get_summary_stats(
  stat = "mean",
  variability = "ci",
  conf_level = 0.90
)

# Apply to a vector
my_summary_fun(rnorm(100, mean = 10, sd = 2))
}

}
\keyword{internal}
