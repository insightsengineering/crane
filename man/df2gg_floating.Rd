% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_utils.R
\name{df2gg_floating}
\alias{df2gg_floating}
\title{Convert Data Frame to Floating ggplot2 Table Graphic}
\usage{
df2gg_floating(
  df,
  gg_plt,
  x = 0.8,
  y = 0.8,
  w = 0.3,
  h = 0.2,
  colwidths = NULL,
  font_size = 10,
  col_labels = TRUE,
  col_lab_fontface = "bold",
  hline = TRUE,
  bg_fill = NULL
)
}
\arguments{
\item{df}{(\code{data.frame})\cr
The data frame to render.}

\item{gg_plt}{(\code{ggplot2})\cr
The main plot to overlay the table onto.}

\item{x}{(\code{numeric})\cr
X-coordinate for the box anchor position (0 to 1).}

\item{y}{(\code{numeric})\cr
Y-coordinate for the box anchor position (0 to 1).}

\item{w}{(\code{numeric})\cr
Width of the annotation box (0 to 1).}

\item{h}{(\code{numeric})\cr
Height of the annotation box (0 to 1).}

\item{colwidths}{(\code{numeric})\cr
Numeric vector of relative column widths.}

\item{font_size}{(\code{numeric})\cr
Base font size.}

\item{col_labels}{(\code{logical})\cr
Whether to display column labels (header).}

\item{col_lab_fontface}{(\code{character})\cr
String for the font face of column labels (e.g., "bold").}

\item{hline}{(\code{logical})\cr
Whether to draw a horizontal line below the column labels.}

\item{bg_fill}{(\code{character})\cr
Optional color string for the plot background.}
}
\value{
A \code{cowplot} object representing the combined plot and table.
}
\description{
Creates a standalone \code{ggplot2} table and overlays it onto a base plot
using \code{cowplot} at specified coordinates.
}
\examples{
\dontrun{
library(ggplot2)

# 1. Create a base plot
p_base <- ggplot(mtcars, aes(x = wt, y = mpg)) +
  geom_point() +
  theme_classic()

# 2. Create a mock summary table
mock_df <- data.frame(
  Statistic = c("N", "Median", "Mean"),
  Value = c("32", "19.2", "20.1")
)

# 3. Float the table on the plot
df2gg_floating(
  df = mock_df,
  gg_plt = p_base,
  x = 0.75,
  y = 0.75,
  w = 0.35,
  h = 0.25,
  bg_fill = "white"
)
}

}
\keyword{internal}
