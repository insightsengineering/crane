% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_proportion.R
\name{tbl_proportion}
\alias{tbl_proportion}
\title{Proportion Summary}
\usage{
tbl_proportion(
  data,
  variable,
  by = NULL,
  value = NULL,
  method = c("waldcc", "wald", "clopper-pearson", "wilson", "wilsoncc", "agresti-coull",
    "jeffreys", "strat_wilson", "strat_wilsoncc"),
  conf.level = 0.95,
  statistic = c("{n} ({p}\%)", "({conf.low}, {conf.high})"),
  label = "Responders",
  ci_label = NULL,
  estimate_fun = label_roche_number(digits = 1, scale = 100)
)
}
\arguments{
\item{data}{(\code{data.frame})\cr
analysis data.}

\item{variable}{(\code{string})\cr
name of the variable to summarize. May be a logical/binary flag (use
\code{value = TRUE}) or a factor/character with several levels (leave \code{value}
\code{NULL} to report every level).}

\item{by}{(\code{string})\cr
name of the grouping (arm) variable. When \code{NULL}, a single-column summary
is produced and comparison verbs are not applicable.}

\item{value}{(\code{scalar})\cr
for a binary summary, the level counted as a "success" (e.g. \code{TRUE}). When
\code{NULL} (default) every level of \code{variable} is reported, each on its own
estimate/CI row pair.}

\item{method}{(\code{string})\cr
confidence-interval method passed to \code{\link[cardx:ard_categorical_ci]{cardx::ard_categorical_ci()}}. One of
\code{"waldcc"} (Wald with continuity correction, the default), \code{"wald"},
\code{"clopper-pearson"}, \code{"wilson"}, \code{"wilsoncc"}, \code{"agresti-coull"},
\code{"jeffreys"}, \code{"strat_wilson"}, \code{"strat_wilsoncc"}.}

\item{conf.level}{(\code{scalar})\cr
confidence level for the interval. Default is \code{0.95}.}

\item{statistic}{(\code{character})\cr
two-element character vector of glue templates for the estimate row and the
CI row. Default \code{c("{n} ({p}\%)", "({conf.low}, {conf.high})")}.}

\item{label}{(\code{string} or named \code{list})\cr
for a binary summary, the label of the estimate row (default
\code{"Responders"}). For a multinomial summary, an optional named list mapping
level values to labels; unmapped levels use the level value itself.}

\item{ci_label}{(\code{string})\cr
label of the confidence-interval row. Default is built from \code{conf.level}
and \code{method}, e.g. \code{"95\% CI (Wald, with continuity correction)"}.}

\item{estimate_fun}{(\code{function})\cr
formatter applied to the CI bounds. Default
\code{label_roche_number(digits = 1, scale = 100)}.}
}
\value{
a \code{tbl_proportion}/\code{gtsummary} table.
}
\description{
Build a gtsummary table of proportions (with confidence intervals) for a
binary or multinomial variable, split by a grouping variable. The estimate
and its confidence interval are rendered on two rows, matching the layout of
binary-outcome / response summary tables (e.g. RSPT01).

The table is a plain \code{gtsummary} object, so the usual \verb{modify_*()} verbs
apply. Between-group comparisons are added with \code{\link[=add_proportion_difference]{add_proportion_difference()}}
(difference in rates, confidence interval, p-value) and
\code{\link[=add_proportion_odds_ratio]{add_proportion_odds_ratio()}} (odds ratio).

All statistical choices, the confidence-interval \code{method}, the \code{conf.level},
and (for comparisons) the \code{test}, live on these functions rather than in the
calling template. Cosmetic choices, factor labels, category wording,
indentation, live in the template.
}
\examples{
set.seed(1)
df <- data.frame(
  arm = factor(sample(c("A", "B"), 100, TRUE), levels = c("A", "B")),
  rsp = sample(c(TRUE, FALSE), 100, TRUE)
)

# binary responder summary
tbl_proportion(df, variable = "rsp", by = "arm", value = TRUE)

# multinomial breakdown
df$cat <- factor(sample(c("CR", "PR", "SD"), 100, TRUE))
tbl_proportion(df, variable = "cat", by = "arm")

}
\seealso{
\code{\link[=add_proportion_difference]{add_proportion_difference()}}, \code{\link[=add_proportion_odds_ratio]{add_proportion_odds_ratio()}}
}
