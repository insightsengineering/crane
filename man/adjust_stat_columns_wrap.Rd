% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_stat_columns_wrap.R
\name{adjust_stat_columns_wrap}
\alias{adjust_stat_columns_wrap}
\title{Adjust Statistics Columns Wrapping}
\usage{
adjust_stat_columns_wrap(tbl, mode = c("protect", "unprotect"))
}
\arguments{
\item{tbl}{(\code{gtsummary})\cr
A \code{gtsummary} object.}

\item{mode}{(\code{character(1)})\cr
Either \code{"protect"} (replaces whitespace with non-breaking spaces) or
\code{"unprotect"} (replaces non-breaking spaces with standard spaces).
Defaults to \code{"protect"}.}
}
\value{
A modified \code{gtsummary} object.
}
\description{
Toggles standard whitespace to non-breaking spaces (\verb{\\u00A0}) dynamically
in all visible statistics columns of a \code{gtsummary} table's body (or vice versa).
This forces the layout engine to keep statistics (e.g., "12.5 ( 95\%)")
on a single line when protected. Column headers and labels remain unaffected.
}
