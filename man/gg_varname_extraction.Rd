% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gg_utils.R
\name{gg_varname_extraction}
\alias{gg_varname_extraction}
\title{Extract Variable Names from ggplot2 Aesthetic Mappings}
\usage{
gg_varname_extraction(mapping_quo)
}
\arguments{
\item{mapping_quo}{(\code{quosure})\cr
A quosure extracted from a \code{ggplot2} aesthetic mapping list (for example,
\code{gg_plt$mapping$x}).}
}
\value{
A single character string containing the extracted variable name, or
\code{NULL} if the variable cannot be safely extracted or is missing.
}
\description{
This internal helper function safely extracts variable names as character
strings from \code{ggplot2} aesthetic mappings (quosures). It is designed to
handle both simple symbols (e.g., \code{aes(x = TIME)}) and \code{.data} pronouns (e.g.,
\code{aes(x = .data[["TIME"]])}). Complex expressions or mathematical operations
(e.g., \code{aes(x = TIME / 24)}) will safely fall back to returning \code{NULL}.
}
\examples{
\dontrun{
library(ggplot2)

# Create a plot with both standard and .data pronoun mappings
p <- ggplot(mtcars, aes(x = mpg, y = .data[["wt"]]))

# Extracts "mpg"
gg_varname_extraction(p$mapping$x)

# Extracts "wt"
gg_varname_extraction(p$mapping$y)
}

}
\keyword{internal}
